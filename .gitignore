node_modules/
.next/
coverage/
.DS_Store
*.log
.env*
!.env.example
public/apk/

# Claude Code — local settings (permission allowlist, kadang berisi credentials)
.claude/settings.local.json

# Python bytecode
__pycache__/
*.pyc

# Load test — local results + JWT config (contains tokens)
load_test/results/
load_test/config.json
loadtest/

# Flutter — artefak build lokal (VPS punya /opt/flutter, jadi ini kegenerate
# tiap `flutter analyze`). Jangan ikut ke-commit.
kasir_app/.dart_tool/
kasir_app/.flutter-plugins-dependencies
kasir_app/build/

# TypeScript incremental build info
tsconfig.tsbuildinfo
